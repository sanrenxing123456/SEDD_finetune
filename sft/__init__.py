"""Supervised fine-tuning utilities for conditional SEDD models.

Submodules are intentionally not imported here: data preparation should remain
usable on machines that do not have SEDD's CUDA-only Flash Attention runtime.
"""
